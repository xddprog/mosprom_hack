services:
  frontend:
    build: .
    container_name: poseshchaemostinet-frontend-container
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - SKIP_TYPE_CHECK=true
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
